import { FC } from 'react';

// styles
import styles from '../styles/main.module.scss';

export const Jokes: FC = (): JSX.Element => {

    return (
       <div className={styles.jokes}></div>
    );
};
